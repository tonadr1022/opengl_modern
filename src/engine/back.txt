project(opengl_modern)


set(SOURCES

)

add_library(${PROJECT_NAME} STATIC ${SOURCES})

target_link_libraries(opengl_modern PRIVATE GLEW::GLEW spdlog::spdlog glm::glm
                                            glfw EnTT::EnTT)
